/** @jsx jsx */
import { jsx } from "@emotion/core";
import DemandBlock from "./DemandBlock";

export const NationalDatabaseOfLandlordsAndRents = () => (
  <DemandBlock demand="We demand a national database of landlords and rents." />
);

export const RentControls = () => (
  <DemandBlock demand="We demand rent controls which bring down rents to 30% of local income." />
);

export const WelfareSystemThatSupportsHousing = () => (
  <DemandBlock demand="We demand a welfare system that supports access to safe, secure housing." />
);

export const OpenEndedTenancies = () => (
  <DemandBlock demand="We demand open - ended tenancies and an end to section 21 evictions." />
);